<div class="page-header">
  <h1><%=t('titulos.chamados')%></h1>
</div>
<%
if @teams.blank?
  concat t('messages.times.nenhum')
else
  open_row = false  
  @teams.each do |team|
    open_row = !open_row
    if(open_row) 
%>
        <div class="row">
<%
    end
%>
  <div class="alert alert-info span5" style="min-height: 150px;">
    <h3 style="display:inline;"><%=team.name%></h3> [<a href="<%=edit_team_path(team)%>">edit</a>]<br/>
    <% show_team_projects(team) %>
  </div>
<%
    unless open_row
%>
</div>
<%
    end
  end #fim do each
  if open_row
%>
</div>
<%
  end
end
%>
